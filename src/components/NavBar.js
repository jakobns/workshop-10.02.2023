export default function NavBar({username}){
    return(
        <header>
            <nav>
                <h1>
                    Hiø
                </h1>
            </nav>
            <span>{username}</span>
        </header>
    )
}